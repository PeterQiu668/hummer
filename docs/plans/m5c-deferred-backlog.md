# M5-C deferred backlog

The following items are explicitly outside M5-C and are not started by this milestone:

- Tool registry and active MCP configuration, including a durable `tool_invocations` ledger.
- DeepSeek Harness second execution shell using `@deepseek-ai/dsh-acp`.
- Feishu `ChannelAdapter`.
- Automatic updates.

These items require separate architecture decisions and acceptance gates. Their presence here is not an implementation claim.
